/** site routes */

const { companyRoutes } = require("./company");
const { userRoutes } = require("./user");
const { authRoutes } = require("./auth");

module.exports = { companyRoutes, userRoutes, authRoutes };
